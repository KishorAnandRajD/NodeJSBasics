var person={
  name:'Kishor'
};

console.log(person);

person.age=23;

// Special Line break
debugger;  // No need to use 'n' command to come to this line. Once you use 'c' command, it will directly point to this line

person.name='Anand';

console.log(person);
